# Copy to .env and adjust. Used by platform, devices, and feed subscriber (load_dotenv).

# Broker (defaults: Shiftr.io public)
# MQTT_BROKER_HOST=public.cloud.shiftr.io
# MQTT_BROKER_PORT=1883
# MQTT_USER=public
# MQTT_PASSWORD=public

# Platform: default PIN for keypad devices (key "default" cannot be removed)
# PLATFORM_DEFAULT_PIN=platform-pin

# Platform: enable log mode on startup (1, true, yes)
# PLATFORM_LOG=0
